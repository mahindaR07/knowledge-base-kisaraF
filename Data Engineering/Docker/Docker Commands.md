# Docker Commands

The below has a good comprehensive list of Docker commands which is really useful to work with Docker.


## 1. Docker Run `<service>`
- For running an instance of the software
- Using the option __-d__ will run the container in a detached mode

## 2. Docker PS
- To list all the containers running with info on each
- Each container has a name and a random unique ID


Let's just change the EOL
